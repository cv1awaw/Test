import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: "Cross origin request" warning in dev is expected when accessing via IP.
};

export default nextConfig;
